# NGC4254 cross-wedge gas-dynamics remainder v05

Off-wedge `m0+m1+m2` fits predict the held-out major-axis wedge. Five radial residual contrasts give `chi2=55.15/5` (`p=1.214e-10`); maximum absolute residual is `4.61 km/s`.

This is a spatial holdout nuisance test using the same endpoint field, not an independent source-body prediction. Any surviving residual remains unqualified as a channel signal.
