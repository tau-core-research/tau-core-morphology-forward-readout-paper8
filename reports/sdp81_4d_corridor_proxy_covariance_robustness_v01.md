# SDP.81 4D-corridor proxy covariance robustness v01

Status: `DIAGNOSTIC_ONLY_NOT_ENDPOINT`. Verdict: `WEAK_PATH_ORDER_COMPATIBILITY_NOT_MODEL_SELECTION`.

The fixed proxy beats lossless transfer for `6/9` blank-aperture geometries, beats the wrong-assignment median for `9/9`, and beats every wrong assignment for `0/9`. The fraction of wrong assignments no worse than matched spans `0.391` to `0.478`. In the descriptive scale scan, the frozen unit scale is best for `2/9` geometries and scale 0.5 is best for `7/9`; post-endpoint retuning is forbidden.

Retrospective covariance robustness for the already frozen nonphysical 4D proxy. The path-order advantage over a wrong-assignment median is descriptive, while preference over lossless transfer is not stable across blank-aperture geometries and no geometry beats every wrong assignment. This is neither a calibrated population p-value nor evidence for a Nature-occupied parent action.
