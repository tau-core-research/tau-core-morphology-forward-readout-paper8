# COMING profile reverse-render validation v01

Status: `VISIBLE_PROFILE_REVERSE_RENDER_PASS_CLIPPED_OUTLIER_UNCERTAINTY_OPEN`

All `56` visible points pass the physical-to-vector round trip at `0.0002` pt tolerance. Maximum marker residuals are `8.11e-05` pt in x and `7.32e-05` pt in y.

The separate NGC4579 `Delta=2.9` source-text point is retained, but its clipped uncertainty cannot be reverse-rendered. The visible geometry gate passes; the strict uncertainty-complete source gate and endpoint scoring remain closed.
