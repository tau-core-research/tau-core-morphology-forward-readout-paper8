# NGC4579 SINGS Halpha rotation extraction freeze v01

Status: `INDEPENDENT_HALPHA_ROTATION_EXTRACTION_PROTOCOL_FROZEN_BEFORE_FIGURE_VALUES_OPEN`

Daigle et al. (2006) Appendix Figure C2 is selected as the replacement
independent-Halpha carrier before opening its NGC4579 rotation values. The
approaching and receding source markers will be retained separately, with the
primary domain fixed at `R >= 3.62 kpc`. The already frozen three-mode
modal-capacity operator may not be changed after extraction.

This freeze does not assert that the graphical endpoint will pass coverage or
reverse-render gates and does not authorize scoring.
