# SDP.81 common-action forward-model compiler v01

Status: `FORMULA_SHELL_DERIVED_ENDPOINT_BLOCKED`.

The source-only compiler reads no spectral or velocity endpoint. It is ready to reduce one physically authorized `(B,u,I)` common Hessian per path into the selected fixed-lift or relaxed-lift transfer and a five-mode centered-log terminal calibration.

Current blocker: `physical common-action manifest absent`. Endpoint authorized: `False`. The physical manifest is absent, so no CO cube was opened and no score was emitted.

Even after future authorization the SDP.81 result will be retrospective: the spectral endpoint predates this formula freeze. The first confirmatory claim requires a new untouched multipath endpoint.
