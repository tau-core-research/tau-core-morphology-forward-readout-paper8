# SDP.81 continuum path registration v01

Verdict: `CONTINUUM_PATH_REGISTRATION_GRID_NOT_CLOSED`

Selected path shifts: `[(-20.0, 0.0), (-20.0, 40.0), (-40.0, 40.0), (-20.0, 40.0)]` mas. `3` of four paths select a grid boundary.

Each calibration is held out from the three-path continuum source fit and uses no held-out source gain. Transfer to Band-6 is authorized only when all selected solutions are interior to the frozen grid.
