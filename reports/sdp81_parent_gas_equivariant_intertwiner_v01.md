# SDP.81 parent--gas equivariant-intertwiner audit v01

Status: `SYMMETRY_INTERTWINER_SHAPE_DERIVED_CURRENT_SYMMETRIES_NONSELECTING`. Checks: `14/14`. No spectral endpoint or CO(10-9) header/pixels were read.

A common source-owned symmetry would require $P\rho_Z(g)=\rho_\xi(g)P$. The actual ordered $P_6$ reflection has centered parity split `2+3` and a `13`-dimensional intertwiner space, so it does not select the bridge. A hypothetical standard $S_6$ action leaves a `1`-dimensional scalar commutant, but is neither a symmetry of the ordered graph nor a sourced gas representation, and its scale remains free.

Generic distinct gas components are not fixed by label exchange; its fixed locus has radiative rank `2`. The physical common representation/intertwiner and nonzero susceptibility anchor remain open.
