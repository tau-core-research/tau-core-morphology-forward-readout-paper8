# NGC4579 SINGS Figure C2 side extraction freeze v01

Status: `SIDE_RESOLVED_FIGURE_C2_EXTRACTION_PROTOCOL_FROZEN_BEFORE_PANEL_VALUES_OPEN`

The full 44-page ADS journal PDF is checksum-frozen. Figure C2 is on PDF page
43 and places NGC4579 in the bottom-right panel. Before opening that panel's
values, the extraction is restricted to source-identifiable approaching and
receding marker series, with primary support fixed at `R >= 45.253 arcsec`.

The author numeric average may calibrate axes but may not be inverted to invent
side values. At least three markers per side and a passing reverse render are
required before any orientation-odd diagnostic.
