# PHANGS morphology-orthogonal tracer velocity-field test

Status: `ELIGIBLE_THREE_GALAXY_MORPHOLOGY_ORTHOGONAL_NULL_REPLICATED`

After source-frozen morphology removal, the retained orthogonal field gives `chi2=23.17` for `30` dof (`p=0.8082`). The wrong-family control leaves `chi2=36.98` for `30` dof (`p=0.1777`).

The eligible three-galaxy matched-removal statistic remains consistent with zero. The expanded wrong-family control is also consistent with zero, so the original two-object family-specificity hint does not replicate. NGC3627 adds a stress control, but its source-known bar plus interaction occupy both tested angular families, leaving no low-order morphology-orthogonal direction. No common channel mode is identified.
